# Core — no fixed upper bounds, so pip can resolve whatever's current and
# compatible on your machine. All CPU-only, no GPU/CUDA required anywhere
# in this project.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44
streamlit>=1.28
plotly>=5.18
mlxtend>=0.22
matplotlib>=3.7
joblib>=1.3

# Backend API layer (optional but recommended — demonstrates REST API skills
# on top of the DS pipeline; the dashboard and pipeline scripts don't need it)
fastapi>=0.110
uvicorn>=0.27

# Optional — data download & extra model. The pipeline runs fine without
# these; download_data.py falls back to manual instructions and
# forecasting.py skips LightGBM automatically if it's not installed.
ucimlrepo>=0.0.7
requests>=2.31
lightgbm>=4.0
